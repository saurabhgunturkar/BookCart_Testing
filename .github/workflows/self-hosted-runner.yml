name: Self Hosted Runner

on: [push]

jobs:
        build:
            runs-on: self-hosted
            steps:
                - name: Checkout Code
                  uses: actions/checkout@v3
                - name: Run Build
                  run: echo "Building on self-hosted runner!"
  